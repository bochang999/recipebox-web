{
  "name": "recipebox-web",
  "version": "1.0.0",
  "description": "## 概要 分量調整と塩分濃度計算ができるシンプルな料理レシピ管理Webアプリ",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && cp index.html style.css script.js manifest.json sw.js dist/",
    "build:production": "npm run build && npx sentry-cli releases files $npm_package_version upload-sourcemaps dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "@sentry/capacitor": "^2.2.0",
    "@sentry/wizard": "^6.3.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
